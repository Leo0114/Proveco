---
import Container from "@components/Container.astro";
import { Image } from "astro:assets";
import mascotasImage from "@assets/images/dogs.webp";
---

<Container>
  <section
    class="relative overflow-hidden bg-gradient-to-b from-primary to-aFondo md:bg-gradient-to-br md:from-primary md:via-aFondo md:to-primary rounded-3xl xl:rounded-[2.5rem] shadow-lg hover:shadow-xl transition-shadow duration-300"
  >
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
      <!-- Contenido textual -->
      <article class="p-8 md:p-12 lg:p-16 order-2 md:order-1">
        <header>
          <h2
            class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 mb-4 leading-tight"
          >
            <span
              class="block text-2xl md:text-3xl text-aFondo font-semibold mb-2"
            >
              Cuidado especializado para
            </span>
            Tus Mascotas
          </h2>
        </header>

        <p class="text-lg md:text-xl text-gray-600 mb-8 md:mb-12 max-w-prose">
          En Proveco Pets entregamos soluciones integrales para la salud y
          bienestar de tus compañeros peludos. Calidad certificada y amor animal
          en cada producto.
        </p>

        <div class="flex flex-col sm:flex-row gap-4">
          <button
            class="bg-primary hover:bg-primary-dark text-white px-8 py-4 rounded-full font-semibold text-lg transition-all transform hover:scale-105 focus:outline-none focus:ring-4 focus:ring-primary/20"
            aria-label="Ver productos para mascotas"
          >
            Ver productos
          </button>
          <button
            class="border-2 border-primary text-primary hover:bg-primary/10 px-8 py-4 rounded-full font-semibold text-lg transition-all transform hover:scale-105 focus:outline-none focus:ring-4 focus:ring-primary/20"
            aria-label="Más información sobre Proveco Pets"
          >
            Conoce más
          </button>
        </div>
      </article>

      <!-- Contenedor de imagen responsive -->
      <div class="relative h-96 md:h-auto order-1 md:order-2">
        <Image
          src={mascotasImage}
          alt="Mascotas felices: perro y gato jugando"
          class="object-cover w-full h-full rounded-xl"
          widths={[600, 800, 1200]}
          loading="lazy"
        />
        <div
          class="absolute inset-0 bg-gradient-to-r from-white/30 to-transparent md:bg-gradient-to-l"
        >
        </div>
      </div>
    </div>
  </section>
</Container>
